#include "/usr/local/include/pbc/pbc.h"

#ifndef _TATE2WEIL
#define _TATE2WEIL
void tate2weil(element_t *out,element_t in1,element_t in2,pairing_t pairing);
//to turn the tate pairing into the weil pairing
#endif
